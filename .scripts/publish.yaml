# Node.js
# Build a general Node.js project with npm.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- master 

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '10.x'
  displayName: 'Install Node.js'

- script: |
    npm install -g npm 
    npx @microsoft/rush update 
    npx @microsoft/rush sync-versions
    npx @microsoft/rush set-version 
    npx @microsoft/rush sync-versions
    npx @microsoft/rush update 
    npx @microsoft/rush rebuild 

    # build the packages
    npx @microsoft/rush publish --publish --pack --include-all --tag preview

    # publish the packages
    echo "//registry.npmjs.org/:_authToken=$(azure-sdk-npm-token)" > ./.npmrc 
    for file in common/temp/artifacts/packages/*.tgz 
    do
     common/temp/pnpm-local/node_modules/.bin/pnpm publish $file --tag preview --access public || echo no-worries 
    done

